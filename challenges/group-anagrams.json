{
  "id": "group-anagrams",
  "title": "Group Anagrams",
  "difficulty": "medium",
  "tags": ["strings", "hash-map"],
  "estimatedMinutes": 20,
  "description": "Given an array of strings `strs`, group the anagrams together and return the groups in any order.\n\nAn **anagram** is a word formed by rearranging the letters of another word using all original letters exactly once.\n\n**Key Insight:** Two strings are anagrams if and only if their sorted character sequences are identical. Use the sorted string as a hash map key to group anagrams.\n\n**Constraints:**\n- `1 <= strs.length <= 10^4`\n- `0 <= strs[i].length <= 100`\n- `strs[i]` consists of lowercase English letters.",
  "examples": [
    {
      "input": "strs = [\"eat\", \"tea\", \"tan\", \"ate\", \"nat\", \"bat\"]",
      "output": "[[\"bat\"], [\"nat\", \"tan\"], [\"ate\", \"eat\", \"tea\"]]",
      "explanation": "\"eat\", \"tea\", \"ate\" share sorted key \"aet\". \"tan\", \"nat\" share \"ant\". \"bat\" is alone."
    },
    {
      "input": "strs = [\"\"]",
      "output": "[[\"\"]]",
      "explanation": "Single empty string forms its own group."
    }
  ],
  "starterCode": {
    "javascript": "function solution(strs) {\n  // your code here\n}",
    "python": "def solution(strs):\n    # your code here\n    pass"
  },
  "testCases": {
    "visible": [
      { "input": { "strs": ["eat", "tea", "tan", "ate", "nat", "bat"] }, "expected": [["bat"], ["nat", "tan"], ["ate", "eat", "tea"]] },
      { "input": { "strs": [""] }, "expected": [[""]] },
      { "input": { "strs": ["a"] }, "expected": [["a"]] }
    ],
    "hidden": [
      { "input": { "strs": ["abc", "bca", "cab", "xyz", "zyx"] }, "expected": [["abc", "bca", "cab"], ["xyz", "zyx"]] },
      { "input": { "strs": ["ab", "ba", "cd", "dc", "ef"] }, "expected": [["ab", "ba"], ["cd", "dc"], ["ef"]] },
      { "input": { "strs": ["a", "b", "c"] }, "expected": [["a"], ["b"], ["c"]] }
    ]
  },
  "editorial": "Build a map from sorted-string key to list of originals. For each string, compute key = sorted characters, append original to map[key]. Return all map values. Time: O(n * k * log k) where k is max string length. Space: O(n * k).",
  "rexHints": {
    "level1": "Anagrams contain the same characters — think about what canonical form they all share.",
    "level2": "Sort each string's characters to get a key. Strings with the same sorted key are anagrams. Group them in a hash map.",
    "level3": "map = {}. For each str: key = str.split('').sort().join('') (JS) or ''.join(sorted(str)) (Python). Push str into map[key]. Return Object.values(map) or list(map.values())."
  }
}
